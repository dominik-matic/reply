#include <bits/stdc++.h>
#include "structs.hpp"
#include "parser.hpp"

using namespace std;

int main(){

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    Data data = parseData();

    // ofstream outputFile;
    // string outputFileName="";

    // outputFile.open(outputFileName);
    
    return 0;
}